version: "3.9"
services:
  worker:
    build:
      context: ../apps/worker
    env_file:
      - ../.env
    volumes:
      - ../apps/worker:/app
      - tmp-media:/app/tmp
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  tmp-media:
